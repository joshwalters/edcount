cmake_minimum_required(VERSION 2.8)
project(edcount)
set(BIN_DIR ${edcount}/bin)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)
file(GLOB SOURCE_FILES src/*.c)
add_executable(edcount ${SOURCE_FILES})
